name: Go

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: [1.20]
        platform: [linux/amd64]
        os: [ubuntu-latest]
        include:
          - platform: linux/amd64
            goarch: amd64
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: ${{ matrix.go-version }}
    - name: Build for amd64
      run: |
        GOARCH=amd64 go build -v -o ./bin/go-amd64
    - name: Upload artifacts
      uses: actions/upload-artifact@v2
      with:
        name: go-amd64
        path: ./bin/go-amd64.zip
